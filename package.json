{
  "name": "it-community-app",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "server",
    "shared"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=server",
    "server": "npm run dev --workspace=server",
    "install:all": "npm install && npm install --workspace=server && npm install --workspace=shared"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

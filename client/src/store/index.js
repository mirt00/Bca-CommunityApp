/**
 * Store barrel export.
 * Import stores from here to keep import paths clean.
 *
 * @example
 *   import { useAuthStore, useChatStore } from '../store';
 */
export { useAuthStore } from './authStore';
export { useChatStore } from './chatStore';
